@page "/movies/edit/{MovieId:int}"

<h3>Edit Movie</h3>
@if (Movie != null)
{
    <MovieForm Movie="Movie" OnValidSumbit="Edit" SelectedGenres="Selected"  NotSelectedGenres="NotSelected"/>
}

@code {
    [Parameter] public int MovieId { get; set; }
    private Movie Movie;

    private List<Genre> NotSelected = new List<Genre>()
    {
        new Genre() {Id=2, Name="Comedy"},
        new Genre() {Id=3, Name="Drama"}
    };

    private List<Genre> Selected = new List<Genre>()
    {
        new Genre() {Id=1, Name="Action"}
    };

    protected override void OnInitialized()
    {
        Movie = new Movie() { Id = MovieId, Title = "MOOOOV" };
    }

    private void Edit()
    {
        //do
    }

}
